# Base Lowering

Lowers the AST we introduced and type checked in [types/base](/types/base).
This code is covered by parts 5a & 5b of [making a language series](https://thunderseethe.dev/series/making-a-language/) 

Main entry point to the lowering pass is the `lower` function.
