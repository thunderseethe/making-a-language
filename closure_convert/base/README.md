# Closure Convert Base

Covered by [this post](https://thunderseethe.dev/posts/closure-convert-base/) in the making a language series.
Closure conversion makes the implicit capture of our lambda functions explicit and easier to compile.
Lambda functions are replaced by closures, which are structs that hold a top level function and the free variables captured by the lambda.

