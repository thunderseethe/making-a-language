[workspace]
members = [
  "lowering/base",
  "lowering/rows",
  "lowering/items",
  "types/base",
  "types/items",
  "types/rows", "simplification/base", 
]

[workspace.dependencies]
types-base = { path = "types/base" }
types-rows = { path = "types/rows" }
types-items = { path = "types/items" }
lowering-base = { path = "lowering/base" }
ena = "0.14.2"
im = "15.1"
pretty_assertions = "1.4"

[package]
name = "type-inference"
version = "0.1.0"
edition = "2021"
